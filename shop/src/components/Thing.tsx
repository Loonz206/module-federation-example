import React from "react";
import styles from './Thing.module.scss';

const Thing = () => {
  return (
    <div>
      <h1 className={styles.error}>Hello App Component</h1>
    </div>
  );
};

export default Thing;
